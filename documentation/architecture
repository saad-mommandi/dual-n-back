Frontend:
Tools: CSS and Javascript. Decided to not use a JS framework as the app is simple and my focus is not becoming a JS dev.

Preloading audio: Audio is preloaded as there will be inherent latency if every audio file delivered via an API
TODO: revisit to explore caching mechanisms or preload from a different location via an API call (maybe a microservice to return all audio from a folder?)

As the application grows, there may be issues w scalability/dependency management/using the global namespace
TODO: research factoring out the application into modules instead of just files in the global NS

core logic:
The core n-back trial logic is run on the front-end as it is time sensitive and network latency is unacceptable 




